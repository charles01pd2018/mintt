import { getClientCoords, getRectSize } from './events';
import { colors, background, shadow } from './constants';

export {
    getClientCoords,
    getRectSize,
    colors,
    background,
    shadow,
}